import 'package:flutter/material.dart';

// class Login extends StatelessWidget {
//   const Login({super.key});

//   @override
//   Widget build(BuildContext context) {
//     return const Center(
//       child: Text("Tela de Login"),
//     );
//   }
// }

class Home extends StatelessWidget {
  const Home({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Tela de Home"),
    );
  }
}



class PesquisarLivro extends StatelessWidget {
  const PesquisarLivro({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Tela de Pesquisar Livro"),
    );
  }
}

class Emprestimo extends StatelessWidget {
  const Emprestimo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Tela de Empréstimo"),
    );
  }
}

class Devolucao extends StatelessWidget {
  const Devolucao({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Tela de Devolução"),
    );
  }
}

class Autores extends StatelessWidget {
  const Autores({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Tela de Autores"),
    );
  }
}

class Livros extends StatelessWidget {
  const Livros({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Tela de Livros"),);
  }
}

class Relatorios extends StatelessWidget {
  const Relatorios({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Tela de Relatórios"),
    );
  }
}

class NadaConsta extends StatelessWidget {
  const NadaConsta({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Tela de Nada Consta"),
    );
  }
}

class Usuarios extends StatelessWidget {
  const Usuarios({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Tela de Usuários"),
    );
  }
}

class Configuracoes extends StatelessWidget {
  const Configuracoes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Tela de Configurações"),
    );
  }
}
